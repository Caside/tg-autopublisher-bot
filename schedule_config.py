"""
Конфигурация расписания для автоматической публикации постов.
Этот файл содержит настройки для определения времени публикации постов.
"""

# Настройки расписания публикаций
SCHEDULE_CONFIG = {
    # Включить или выключить автоматические публикации
    "enabled": True,
    
    # Дни недели для публикаций (0 = понедельник, 6 = воскресенье)
    # Пример: [0, 1, 2, 3, 4] - публиковать только в рабочие дни
    # Пример: [0, 1, 2, 3, 4, 5, 6] - публиковать каждый день
    "days_of_week": [0, 1, 2, 3, 4, 5, 6],
    
    # Интервал между публикациями в минутах
    # Пример: 60 - публикации раз в час
    # Пример: 120 - публикации раз в два часа
    "interval_minutes": 120,
    
    # Время начала публикаций (часы, минуты)
    "start_time": {
        "hour": 12,  # 12:00
        "minute": 0
    },
    
    # Время окончания публикаций (часы, минуты)
    "end_time": {
        "hour": 22,  # 22:00
        "minute": 0
    },
    
    # Альтернативно, можно указать конкретные времена для публикаций
    # Если указано, настройки interval_minutes, start_time и end_time игнорируются
    # Пример: [{"hour": 12, "minute": 0}, {"hour": 15, "minute": 30}, {"hour": 19, "minute": 0}]
    "specific_times": None,
    
    # Для генерации постов во время запуска (вместо ожидания первого интервала)
    # Добавит публикацию сразу при запуске (если текущее время в рамках расписания)
    "generate_on_startup": True,
    
    # Отключаем кэширование постов - каждый пост будет генерироваться отдельно
    "use_post_cache": False,
    
    # Максимальное количество постов для предварительной генерации (используется только если use_post_cache=True)
    "max_cached_posts": 0
} 